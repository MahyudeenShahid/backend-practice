

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 40px;
        }
        .edit-card {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 24px rgba(76, 81, 255, 0.15);
            padding: 32px 24px;
            width: 340px;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0 auto;
        }
        .edit-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 18px;
            box-shadow: 0 4px 16px rgba(76, 81, 255, 0.12);
        }
        .edit-name {
            font-size: 1.3rem;
            font-weight: 700;
            color: #4c51ff;
            margin-bottom: 8px;
        }
        .edit-email {
            font-size: 1rem;
            color: #555;
            margin-bottom: 16px;
        }
        .edit-info {
            font-size: 0.95rem;
            color: #888;
            margin-bottom: 18px;
        }
        .edit-form label {
            font-weight: 600;
            color: #4c51ff;
            margin-bottom: 6px;
        }
        .edit-form input {
            width: 100%;
            padding: 8px 12px;
            border-radius: 6px;
            border: 1px solid #d1d5db;
            margin-bottom: 14px;
            font-size: 1rem;
        }
        .edit-btn {
            padding: 10px 0;
            width: 100%;
            border: none;
            border-radius: 6px;
            font-weight: 700;
            cursor: pointer;
            background: linear-gradient(90deg, #4c51ff 0%, #667eea 100%);
            color: #fff;
            font-size: 1rem;
            box-shadow: 0 2px 8px rgba(76, 81, 255, 0.10);
            transition: background 0.2s, box-shadow 0.2s, transform 0.2s;
        }
        .edit-btn:hover {
            background: linear-gradient(90deg, #667eea 0%, #4c51ff 100%);
            box-shadow: 0 4px 16px rgba(76, 81, 255, 0.18);
            transform: translateY(-2px) scale(1.05);
        }
    </style>
    <title>Edit User</title>
</head>
<body>
    <div class="edit-card">
        <img class="edit-avatar" src="<%= user.avatar || ('https://i.pravatar.cc/80?u=' + user.email) %>" alt="Avatar">
        <div class="edit-name"><%= user.username %></div>
        <div class="edit-email"><%= user.email %></div>
        <div class="edit-info">ID: <%= user.id %></div>
        <form class="edit-form" action="/users/<%= user.id %>?_method=PATCH" method="POST">
            <label for="username">Username</label>
            <input type="text" id="username" name="username" value="<%= user.username %>" required>
            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="<%= user.email %>" required>
            <label for="avatar">Avatar URL</label>
            <input type="text" id="avatar" name="avatar" value="<%= user.avatar %>">
            <button type="submit" class="edit-btn">Update</button>
        </form>
    </div>
</body>
</html>