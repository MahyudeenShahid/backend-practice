<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Count</title>
    <style>
        body {
           background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Segoe UI', Arial, sans-serif;
        }
        .show-users-btn {
            background: linear-gradient(90deg, #2d6cdf 60%, #4f8cff 100%);
            color: #fff;
            border: none;
            padding: 14px 32px;
            border-radius: 8px;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(45,108,223,0.12);
            transition: background 0.2s, transform 0.2s;
            margin-bottom: 24px;
        }
        .show-users-btn:hover {
            background: linear-gradient(90deg, #1a4fa0 60%, #2d6cdf 100%);
            transform: translateY(-2px) scale(1.04);
        }
         .delete-btn {
            background: linear-gradient(90deg, #ff4c4c 0%, #ff7676 100%);
            color: #fff;
        }

        .delete-btn:hover {
            background: linear-gradient(90deg, #ff7676 0%, #ff4c4c 100%);
            box-shadow: 0 4px 16px rgba(255, 76, 76, 0.18);
            transform: translateY(-2px) scale(1.05);
        }
        .user-btn {
            padding: 14px 32px;
            
            font-size: 1.2rem;
            
            cursor: pointer;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s, box-shadow 0.2s, transform 0.2s;
            box-shadow: 0 2px 8px rgba(76, 81, 255, 0.10);
            
            outline: none;
        }
    </style>
    
</head>
<body>
    
    <div style="max-width: 500px; margin: 40px auto; padding: 30px; background: #f8f9fa; border-radius: 12px; box-shadow: 0 4px 16px rgba(0,0,0,0.08); text-align: center;">
        <h1 style="color: #2d6cdf; margin-bottom: 10px;">User Count</h1>
        <p style="font-size: 2.5rem; color: #333; font-weight: bold; margin-bottom: 20px;"><%= userCount %></p>
        <hr style="margin: 24px 0;">
        <div>
        <button class="show-users-btn" onclick="window.location.href='/users'">Show Users</button>
        <div style="text-align: center; margin-bottom: 32px;">
        <a href="/users/new">
            <button class="user-btn delete-btn" style="font-size: 1.1rem;">
                Add User
            </button>
        </a>
    </div>
    </div>
        <h2 style="color: #444; margin-bottom: 8px;">Developed by Mahyudeen Shahid</h2>
        <p style="color: #666; font-size: 1.1rem;">My first backend app with MySQL database</p>
    </div>
</body>
</html>