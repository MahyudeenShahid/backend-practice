<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chit Chat</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
    
    <div class="container">
        <h1>Welcome to Chit Chat</h1>
        <a href="/chats/new" style="text-decoration: none;" ><button class="chat-create-btn">Create New Chat</button></a>

        <% for (const element of chats) { %>
            <div class="chat-message">
                
                <div style="display: flex; align-items: center; justify-content: space-between; width: 80%; flex: 1; gap: 12px;">
                    <div style="display: flex; flex-direction: column; align-items: flex-start;">
                        <span class="chat-from"><%= element.from %></span>                       
                    </div>
                    <div style="display: flex; width: 60%; flex-direction: column; align-items: center;">
                        <div style="display: flex; width: 100%; align-items: center; justify-content: space-between;">
                        <span class="chat-date"><%= element.createdAt.toString().split(" ")[4] %></span>
                        <span class="chat-date"><%= element.createdAt.toString().split(" ").slice(0,4).join(" ") %></span>
                        </div>
                    <span class="chat-text"><%= element.message %></span>
                    </div>
                    <div style="display: flex; flex-direction: column; align-items: flex-end;">
                        <span class="chat-to">→ <%= element.to %></span>
                    </div>
                </div>
            
            <div class="chat-actions">
                <a href="/chats/<%= element._id %>/edit">
                    <button class="chat-update-btn">Update</button>
                </a>
                <form action="/chats/<%= element._id %>?_method=DELETE" method="POST" style="margin-top: 8px;">
                    <button type="submit" class="chat-delete-btn">Delete</button>
                </form>
            </div>
            </div>
        <% } %>
    </div>
</body>
</html>
